# Security policy

Do not open a public issue for a suspected vulnerability. Send a minimal report to
`security@agentshield.dev` with affected versions, reproduction steps, and impact. Never attach
real credentials or personal data. Critical reports are acknowledged within two business days.

AgentShield performs static analysis and does not guarantee that a component is safe. Target code
is never executed by the scanner. Cloud upload is disabled by default.
